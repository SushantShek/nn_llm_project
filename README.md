# NN Assignment

This project contains a complete solution for Exercises 1–5 and bonus tasks.
See documentation inside source files and run via:

```
pip install -r requirements.txt
pip install langchain-community
pip install langchain-classic
pip install -U langchain-huggingface
python src/main.py
```

apiVersion: v1
kind: Secret
metadata:
  name: openai-secret
  namespace: person-finder
type: Opaque
data:
  # Base64 encoded OPENAI_API_KEY
  # You can generate this using: echo -n 'your-api-key' | base64
  API_KEY: REPLACE_WITH_BASE64_KEY
